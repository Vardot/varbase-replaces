# Varbase Replaces

List of no longer wanted packages to be replaced via Composer.

---

Use `"vardot/varbase-replaces": "~10.1.0"` 
> with **Varbase `~10.1.0`** and **Drupal `~11`**

---

Use `"vardot/varbase-replaces": "~10.0.0"` 
> with **Varbase `~10.0.0`** **CKEditor `5`** and **Drupal `~10`**

---

Use `"vardot/varbase-replaces": "~9.2.0"`
> with **Varbase `~9.1.0`** **CKEditor `5`** and **Drupal `~10`**

---

Use `"vardot/varbase-replaces": "~9.1.0"`
> with **Varbase `~9.1.0`** **CKEditor `4`**  and **Drupal `~10`**

---

In case of needing to manage project's local replaces only

NO replaces **for Varbase ~10.1.0**
> With **Drupal 11** :
> Use the `"vardot/varbase-replaces": "10.1.0.0"` static version to have no replaces and manage local replaces in the project with a copy of replaces from https://github.com/vardot/varbase-replaces/blob/10.1.x/composer.json
---

NO replaces **for Varbase ~10.0.0**
> With **CKEditor 5** and **Drupal 10** :
> Use the `"vardot/varbase-replaces": "10.0.0.0"` static version to have no replaces and manage local replaces in the project with a copy of replaces from https://github.com/vardot/varbase-replaces/blob/10.0.x/composer.json


---

NO replaces **For Varbase ~9.1.0**
> With **CKEditor 4** and **Drupal 10** :
> Use the `"vardot/varbase-replaces": "9.1.0.0"` static version to have no replaces and manage local replaces in the project with a copy of replaces from https://github.com/vardot/varbase-replaces/blob/9.1.x/composer.json


---

NO replaces **For Varbase ~9.1.0**
> With **CKEditor 5** and **Drupal 10** :
> Use the `"vardot/varbase-replaces": "9.2.0.0"` static version to have no replaces and manage local replaces in the project with a copy of replaces from https://github.com/vardot/varbase-replaces/blob/9.2.x/composer.json
